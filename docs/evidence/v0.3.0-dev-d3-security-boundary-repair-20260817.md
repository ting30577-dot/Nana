# v0.3.0-dev D3 security-boundary repair evidence

Date: 2026-08-17

Current acceptance status: `acceptance_pending`; `d3_complete=false`

Technical slice status: `codex_only_accept`

## Authority and evidence semantics

- `docs/CURRENT_D3_AUTHORITY.md`, README, the D3-09 machine gate and Vault
  07/10/11/12 all state the same pending conclusion.
- Historical completion/acceptance records are explicitly marked historical or
  superseded. They remain provenance and cannot override the current gate.
- `tests/test_evidence_manifests.py` checks current status, `d3_complete`, the
  blocking observation gate, verification counts, historical labels and the
  manifest's `current_worktree` role in addition to file hashes.
- Versioned text is fixed to LF by `.gitattributes`, preventing a fresh Windows
  checkout from invalidating byte-level manifests under `core.autocrlf=true`.
- The 30-minute target-user observation was not fabricated or replaced by an
  automated test. It remains the only open D3 acceptance gate.
- The 13 repository planning exports and the formal Obsidian Vault were
  compared by SHA-256 after synchronization: 13/13 match. The four overwritten
  Vault pages were backed up before replacement.

## Runtime and user-data boundary

- The product launcher defaults to the OS Nana user-data root rather than CWD
  or the repository. Explicit database paths inside the application tree,
  relative roots and reparse components fail closed.
- `.gitignore` covers database WAL/SHM, owner locks, Workspace, usability,
  export, Artifact, log, crash and temporary roots.
- Legacy checkout data was copied, tree-hashed, committed under the OS Nana
  user-data root and then recoverably archived under ignored `backups/`:
  - `workspaces`: 8 files, 1,774,677 bytes,
    `69a27d234494adb8e21018619318d2bda80a4730f4962880b668d801e9a9400b`;
  - `usability_sessions`: 4 files, 450,808 bytes,
    `9abbe269077a21fb0fd3c011745636f11be805a80d48950064d84bb883739bda`.
- The JSON migration receipt contains no absolute path. Source labels, target
  class, recoverable backup label, counts and digests are sufficient to audit.

## Build and future desktop-shell boundary

- `config/release-input-allowlist.json` freezes current PyInstaller inputs and
  the future Tauri trust split. Tauri code remains unauthorized and absent.
- Root-level generated specs remain ignored and outside the delivery;
  `scripts/build_windows.ps1` is the only supported legacy build entry, creates
  an isolated temporary spec under `build/`, and always invokes the output
  auditor. The pre-repair generated spec is retained only in ignored backup.
- The package auditor refuses unknown top-level entries, reparse/symlink paths,
  runtime/user-data directory names, DB/WAL/SHM, owner locks, credential files
  and fixed credential canaries. It writes and rechecks a deterministic package
  manifest.
- Current `dist/Nana`: 212 entries, zero forbidden findings, package-manifest
  digest `e7799935642bb4e94df0cd89ed634a3bf8ec76dbb0a5b04e58817d679c6824d6`.
- `docs/tauri_trust_boundary_contract.md` freezes Rust/Python authority,
  authenticated typed default-deny IPC, navigation/CSP, sidecar pinning,
  Workspace ownership, picker continuity, data retention, redaction and updater
  signing prerequisites before any Tauri implementation.

## Verification snapshot

- Python: 445/445, 0 skipped.
- Strict repair boundary: 21/21 under `-W error::ResourceWarning`.
- Crash-release probe: 3/3 under `-W error::ResourceWarning`; the immediate
  product restart path additionally passed 20/20 consecutive process-kill
  iterations after fixing the read-only probe/retry comparison defect.
- TypeScript: pass.
- Vitest: 67/67; projection self-test: pass; production build: pass.
- Browser release/failure matrix: 27/27, retries=0.
- Consecutive complete journeys: 10/10, retries=0.
- Release package: 212 entries; generated manifest recheck passes.
- Repository evidence manifests: refreshed and checked after this record.

The known PySide6 shutdown warning remains attributable to the frozen legacy UI
stack. It is not suppressed and is not interpreted as a repaired D3 runtime
resource leak.
