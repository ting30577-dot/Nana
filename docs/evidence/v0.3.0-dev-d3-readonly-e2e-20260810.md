# D3 read-only bridge E2E revalidation — 2026-08-10

This revalidates the already implemented D3 read-only React bridge. It does not
open D3-07 Approval, T3 export, or filesystem mutation serving.

## Command and result

From `nana_web`:

```text
npm.cmd run test:e2e
```

The production Vite build completed successfully and System Chrome ran all
`17` Playwright tests with `0` retries and `0` failures.

## Covered boundary

- authenticated `fetch` + `ReadableStream` SSE with Authorization, Origin and
  Last-Event-ID;
- canonical bootstrap/reload and local-only draft discard;
- Run/Action/Receipt trace, `waiting_approval`, `effect_unknown`, and causal
  projection display;
- Finding producer Run and literal Evidence IDs;
- streamed `finding.drafted` facts update the canonical Finding map without a
  degraded projection state;
- unknown canonical Event preservation with a visible degraded-projection /
  upgrade-needed banner, including a future event type under a known Run
  aggregate without a fabricated Run state;
- literal streamed `cancelled` and `budget_exceeded` Run states;
- default-deny anonymous static/config/source-map access;
- reconnect, session expiry, malformed SSE, sparse IDs and duplicate replay;
- visibility restoration rebuilds bootstrap before reopening the read stream;
- production fixture/token/source-map leakage scan;
- keyboard focus, forced colors, reduced motion, responsive reflow, browser
  scale and serious/critical accessibility checks.

The test suite intentionally installs a throwing native `EventSource` sentinel;
the application remains green because it uses the authenticated fetch stream.

The front-end unit/self-test command also passed:

```text
npm.cmd run test
```

`58` Vitest tests passed across projection, SSE and store, followed by a
passing projection self-test. The earlier restricted-environment esbuild
failure was an access-denied process boundary; the same command passed under
the controlled local verification environment.

A targeted scan of the current read-only evidence, pre-gate guard, test and
production build files found zero matches for local absolute paths, Claude/API
credential names, or long Bearer literals. The D3-07 machine flags remained
`unresolved`, `false`, `false`, `false` throughout.

The latest continuation scan also found zero matches for local absolute paths,
Bearer literals, or private-key markers outside the intentionally scoped Claude
adapter tests and historical relay evidence names.
