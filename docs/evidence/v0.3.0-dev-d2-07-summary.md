# v0.3.0-dev D2-07 证据摘要

D2-07 固定 D2 到 D3 的版本化交接：

- `D2RuntimeHandoff` v3、replay fixture 与机器 gate；
- schema v6 append-only `action_authorizations`，绑定完整 ActionHashMaterial、action hash、registry contract digest、authorization source/ref 和 authorization Event；
- running cancel 两阶段收敛：`paused/cancel_requested`，executor Receipt/预算结算后进入 `cancelled`，树终止不可验证则 `orphaned`；
- structured errors 包含 args Artifact persisted-size 与 budget 失败；
- D3 只能消费 Action/Event/Receipt/outbox/artifact projection，不得重新推导授权或绕过 D2；
- Workspace lock 生命周期、第二实例失败关闭和 reconciliation-before-ready 是开放真实 mutation serving 的硬前置门槛；OpenAPI/runtime app 合流仍是 D3 显式决策。

最终补审证据包括：23 项手工 corpus、360 个 D2-effective +100 个 supplemental 场景、30 个真实孙进程 fixture、suspended Job bind、orphaned 预算结算和 admission/executor 双入口 Artifact 边界。

Claude 二次收敛复审对 F1/F4/F5/F6/F7/F8/F10 全部 ACCEPT，并明确 D2-07 可以共同 ACCEPT、没有剩余 D2 blocker。聚焦回归 68 tests、全量 269 tests、compileall、TypeScript strict 与 manifest 全部通过。D2 55 tests 在 ResourceWarning-as-error 模式通过；既有警告已定位到迁移期 PySide6 UI smoke，不涉及 D2 handles。
