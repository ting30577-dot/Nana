# v0.3.0-dev D3 provenance-link evidence — 2026-08-10

The read-only Cockpit now exposes the canonical Run→Action→Receipt trace and
the Finding provenance relation from the bootstrap projection: Finding
`producer_run_id` and its literal `evidence_ids`. The browser renders these
IDs as facts; it does not resolve them through a second authority or infer
missing relationships.

Verification:

- `npm.cmd run test`: 25 Vitest tests passed plus projection self-test.
- `npm.cmd run check`: TypeScript strict check passed.
- `npm.cmd run test:e2e`: 14 Playwright tests passed, including the
  Run/Action/Receipt, Finding→Evidence, and literal negative-Run-state
  assertions.
- D3-07 machine gate remains unresolved; no mutation or external write was
  introduced.
