# v0.3.0-dev D3 local regression and manifest refresh — 2026-08-10

## Scope

This record covers only the already-authorized local D3 read-only bridge and
the fixture-only T2 `python.unittest.locked` D3-06 boundary. It does not
authorize D3-07 Approval, T3 capability registration, mutation-route expansion,
or any Workspace-outside filesystem write.

## Verification

- `python -m compileall nana_sidecar tests scripts`: pass.
- Full Python suite: `386` tests, `2` declared platform skips, `OK`.
- Strict D3/pre-gate boundary with `ResourceWarning` treated as an error:
  `123` tests, `2` declared platform skips, `OK`.
- `npm.cmd run check`: pass.
- `npm.cmd test`: `58` Vitest tests plus the projection self-test, pass.
- `npm.cmd run build`: production Vite build, pass in the controlled local
  verification environment.
- `npm.cmd run test:e2e`: production build plus `17` Playwright read-only tests,
  `0` retries and `0` failures.
- The pre-gate guard now also asserts that the handoff entry point carries the
  current no-Claude continuation boundary and current evidence link; its
  direct module has `5` tests and the aggregate pre-gate boundary has `113`.
- The configured runtime route rejects `RequestApproval`, `DecideApproval`,
  `AuthorizeAction`, `PublishExport`, and `ConsumeApproval` with `422` before
  command-log writes; this is a pre-gate runtime rejection test, not an
  Approval/export implementation.

The first frontend test/build attempt in the restricted process sandbox failed
at esbuild directory access before Vite loaded the project configuration. The
same commands passed in the controlled local verification environment; no
source failure was inferred from the sandbox access error.

## Evidence manifest

The scoped D3-06 manifest was refreshed after the current workspace security
and projection changes:

- `docs/evidence/v0.3.0-dev-d3-06-manifest.txt`: `23` entries, `0` hash errors.
- Recorded manifest digest:
  `0b4c0c25f6522c2ded42063022ebd6205684347b832730938b8bb258a851de5c`.

## Gate preservation

The machine-readable D3-07 gate remains `joint_status=unresolved` with
`implementation_authorized=false`, `capability_registered=false`, and
`filesystem_write_authorized=false`. No Claude call was made for this
continuation, and no external review verdict is implied by this local record.
