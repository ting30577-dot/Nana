# D3-07 Claude relay-only retry evidence — 2026-08-10

Status: `UNRESOLVED` — no Claude verdict received.

## Request and privacy

Codex invoked only:

```text
.\.venv\Scripts\python.exe .\scripts\ask_claude.py
```

with the repository-relative sanitized packet
`docs/claude_d3_07_entry_gate_packet_sanitized.md`.

Packet SHA-256:

```text
0340b56c43e1e0a935645be74d4b89ba09df8911e13e20d79ba596737ae8eab2
```

The packet scan found no absolute path, API key, environment value, username,
machine identifier, or credential. The request explicitly prohibited file
writes and official API/OAuth authorization.

## Transport result

- `NANA_CLAUDE_API_KEY`, `NANA_CLAUDE_BASE_URL`, and `NANA_CLAUDE_MODEL` were
  present; `ANTHROPIC_API_KEY` was absent.
- The adapter returned its connection-failure diagnostic with exit code `2`
  before any review text.
- A credential-free TCP diagnostic to the configured relay host failed with
  Windows `WinError 10013` (socket permission denied).
- DNS resolution succeeded; no `HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY`, or
  `NO_PROXY` variable was present, and the failure occurred before TLS/HTTP at
  the TCP socket layer.
- No official endpoint, OAuth flow, fallback channel, or user relay was used.

## Decision impact

No `ACCEPT`, `VETO`, or structured Claude verdict exists. The D3-07 gate stays
`joint_status=unresolved`; `implementation_authorized`,
`capability_registered`, and `filesystem_write_authorized` remain `false`.
The failure is transport evidence only and must not be interpreted as Claude
disagreement or as a request for new user authorization.
