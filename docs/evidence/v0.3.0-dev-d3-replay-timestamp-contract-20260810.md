# D3 replay timestamp contract evidence — 2026-08-10

The shared projection type now models `occurred_at` as optional because the
frozen D2 handoff replay intentionally omits that field, while live SSE frames
continue to require and validate it. The reducer preserves the omission in
the activity record and fingerprint canonicalization; it never synthesizes a
timestamp. The React Activity view renders `timestamp not recorded` for that
fixture-only omission, keeping the UI truthful without changing the canonical
event.

Vitest covers the omission and the projection self-test continues to replay the
checked-in D2 handoff. TypeScript check passes. No runtime route, transport
authentication, mutation, authorization, capability, or filesystem behavior
was changed.
