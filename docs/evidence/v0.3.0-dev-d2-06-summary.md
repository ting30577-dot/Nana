# v0.3.0-dev D2-06 证据摘要

D2-06 形成两层固定安全门：

- 23 项手工 security corpus：固定 version/seed/trace、明确 evaluator 与 expected reason，覆盖未注册/digest/schema/path/provider/network/process/env/timeout/cancel/output/replay/NEVER_GRANT 等反例；未授权 T3/T4/T4-like 通过数为 0。
- 460 个执行场景，其中 360 个属于当前 D2 有效执行面，100 个 prompt-like 样本只证明 frozen args containment，不冒充不存在的 prompt runtime gate。50 个 canary 只证明 child env/stdout/stderr，完整 Prompt/log/export stable gate 明确未完成。
- Grant/Approval 覆盖 22 个变化 family 并断言精确 reason；Artifact 覆盖 7 个 family 并断言精确 structured error。

机器门文件：

- `fixtures/v0.3.0-dev/d2_security_corpus.json`
- `fixtures/v0.3.0-dev/d2_security_matrices.json`
- `tests/test_d2_security_corpus.py`
- `tests/test_d2_security_matrices.py`

矩阵补审发现并修复：

- Windows `taskkill /T` 失败且 Popen 后绑定存在竞态；改用 suspended-create→Job-bind→resume，30/30 真实孙进程 fixture 在 5 秒内通过。
- args Artifact 原先未校验 persisted size 与授权 `max_artifact_bytes`；admission 和 executor 现分别以 `E_ARGS_ARTIFACT_SIZE` / `E_ARGS_ARTIFACT_BUDGET` 失败关闭。
- credential canary 仅证明 D2 实际存在的 child env/stdout/stderr 边界；D2 没有 prompt/export runtime，不伪称这两个表面的动态泄露证明。

聚焦验证：security corpus 与 quantitative matrices 均通过。Claude 二审 F8 ACCEPT，确认有效/补充/deferred 分类不降低 stable 门槛；D2-06 最终共同 ACCEPT。
