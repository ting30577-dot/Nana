# D3 shutdown-warning boundary revalidation — 2026-08-10

The full Python suite was rerun with `gc.DEBUG_UNCOLLECTABLE` enabled. It
completed `386 tests`, `2` declared platform skips, and `OK`. The uncollectable
sample again starts with `PySide6.QtCore.Property`, `PySide6.QtCore.QMetaObject`,
`Shiboken.ObjectType`, legacy UI modules/functions, and related UI metadata.

The D3 boundary suite (pre-gate guard, Workspace lock, runtime authority,
read-model, journey command/runtime and Claude adapter) completed `123 tests`
with `-W error::ResourceWarning`, `2` declared platform skips, and `OK`.

One grouped invocation transiently observed SQLite `disk I/O error` in the
process-crash recovery case; the case passed in isolation and the complete
123-test strict suite passed on immediate rerun. No production change was
made for that transient observation.

This evidence does not claim the legacy UI warning is fixed. It confirms the
warning remains outside the D3 Workspace lock, SQLite, worker/process and
runtime write path; real D3-07 external writing remains separately gated.
