# D3 Claude transport isolation probe — 2026-08-10

The full sanitized D3-06/D3-07 review request previously failed before any
review text was returned. This was recorded as transport failure, not as a
Claude decision.

Isolation results:

- The configured relay variables are present; their values are not recorded.
- A direct minimal Anthropic-compatible HTTP probe completed successfully.
- `scripts/ask_claude.py` with one small sanitized context completed
  successfully and returned a structured test response.
- The response explicitly stated that it was a connectivity/format probe and
  not a Nana project decision.

Conclusion: the evidence does not support blaming the Codex conversation
context itself. The earlier failure is narrowed to the larger review request,
its timeout/stream path, or relay handling of that request. No implementation
authority changed; D3-07 remains unresolved with all write flags false.
