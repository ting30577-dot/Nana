# D3-06/D3-07 first-party Claude CLI preflight

Date: 2026-08-09  
Status: **AUTHORIZED AND SENT — no usable verdict; see CLI result evidence**

## Local channel facts

- Claude Code CLI version: `2.1.214`
- Windows Authenticode status: `Valid`
- signer subject: `Anthropic, PBC`
- signer certificate thumbprint:
  `0D7581D2C51C59DF686C3000C70BF543F9F6C6CB`
- authentication state: logged in
- authentication method: OAuth token
- API provider reported by the CLI: `firstParty`
- account identity/token value: not read or recorded
- network request performed by this preflight: no

The executable path and account identity were deliberately not recorded. The
valid vendor signature supports local binary provenance; it does not itself
authorize transmitting Nana content or replace transport privacy consent.

This is a materially different destination from the unavailable custom
Anthropic-compatible gateway. The product owner's prior authorization named the
third-party gateway and therefore does not automatically authorize this
first-party channel.

## Exact proposed disclosure

The project context remains byte-for-byte the preflighted combination of:

On Windows, the adapter's exact `Path.relative_to(PROJECT_ROOT)` serialization
uses backslashes in the two relative context headers. Those relative headers
are not absolute paths and are included in the recorded combined-prompt hash.

| File | SHA-256 |
|---|---|
| `docs/claude_d3_06_implementation_exit_packet_sanitized.md` | `81df51cbb08ee5d2dd2e79ed94ed9ef3379d140c91d0625dc03d860a95485c8e` |
| `docs/claude_d3_07_entry_gate_packet_sanitized.md` | `0340b56c43e1e0a935645be74d4b89ba09df8911e13e20d79ba596737ae8eab2` |

- combined context SHA-256:
  `e514296813130b7ae6b7e457b50b81fb8cc2a1480e570bfbf0c4a91f2e1abbc8`
- fixed system prompt SHA-256:
  `34193b2bd081d511f8edae099f45de86ff30aa6dae70823c4f0e08f23a5964ab`
- question SHA-256:
  `bf50840c2f55f4354d55231d5c70072decbbaac225028092fa88fc2a1454fd89`
- complete outbound user-prompt SHA-256:
  `f9c963aa3e7246a98d161c3229287f9d7c51e9953184610383439589c319d17b`

The prompt will be supplied over stdin, not as a command-line argument.

## Exact execution envelope

The process will start from a newly created, verified-empty directory under the
system temporary directory. It will receive no `--add-dir`, file resource,
MCP configuration, plugin directory, continuation/resume session, IDE, Chrome,
remote-control or worktree option.

The exact non-secret argument vector is:

```text
--print --output-format json --no-session-persistence --safe-mode
--tools "" --no-chrome --disable-slash-commands --permission-mode plan
--model sonnet
```

`--system-prompt` receives only the fixed system prompt whose SHA-256 is listed
above. The complete user prompt is written as exact UTF-8 bytes to standard
input and is never supplied as a command-line argument.

Before process start, the child environment removes all `NANA_CLAUDE_*` and
`ANTHROPIC_*` override/key variables. It sets the following nonessential
traffic controls to `1`: `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC`,
`DISABLE_TELEMETRY`, `DISABLE_ERROR_REPORTING`, `DISABLE_BUG_COMMAND` and
`DISABLE_AUTOUPDATER`. OAuth authentication remains in the CLI's local secure
credential mechanism. No value from any environment variable is recorded or
sent as project context.

## Proposed isolation flags

- non-interactive print mode
- `--safe-mode` to disable project/user customizations, hooks, plugins, MCP and
  auto-memory behavior
- an explicit replacement system prompt matching the fingerprint above
- `--tools ""` so Claude receives no file, shell, browser or mutation tool
- `--permission-mode plan`
- `--no-session-persistence`
- JSON output for an auditable result and usage envelope
- no continuation/resume, additional directory, file download, Chrome, IDE,
  remote-control, plugin or MCP option

The CLI is an external first-party service, not a local model. Sending still
discloses the two Nana review packets to Anthropic even though it removes the
unverified third-party gateway from the route.

The authorized exact request was sent and returned no usable verdict because
the response asked to read unavailable local evidence despite the no-tools
restriction. See `docs/evidence/v0.3.0-dev-d3-07-claude-cli-result.md`.

## Authorization boundary

Proceed only after the product owner explicitly authorizes both named packet
contents to be sent through the logged-in first-party Claude Code channel and
acknowledges disclosure of Nana internal implementation/design/test details.
Any packet, prompt, provider or isolation-flag change requires a fresh
preflight.
