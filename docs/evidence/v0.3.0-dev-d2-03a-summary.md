# v0.3.0-dev D2-03a 证据摘要

## 本次范围

D2-03a 修正 D2-03 前置的三个阻塞：

- 持久化 registry 改为保存完整 `CapabilityRegistryEntry` JSON。
- process effect 可通过 `CapabilityConstraints.process_targets` 被 PolicyGrant 精确授权。
- `python.unittest.locked` 增加 canonical read scope：`project:source` 与 `project:tests`。
- 新增 contract/storage round-trip 证据，证明完整 `entry_json` 可重建 `CapabilityRegistryEntry`，且列里的 `executable_digest` / `contract_digest` 与 JSON 内容一致。
- read scope 边界已明确：`project:source` 与 `project:tests` 只覆盖项目拥有的源码/测试树；解释器、stdlib、site-packages 与 import resolver 属于 D2-04 executor/resolver。

## 当前验证

- `python -m unittest tests.test_vnext_contracts tests.test_vnext_storage tests.test_vnext_sidecar -v`：功能性通过，仅在 manifest 更新前报告 manifest hash 过期。
- 最终补证固定 built-in digest，并证明 schema v4 写入的 full registry JSON 经 v5/v6 migration 后 bit-for-bit 不漂移；Claude F10 ACCEPT。

## 边界

本次仍不实现 D2-03b admission service、D2-04 executor、Receipt 或 runtime budget accounting。
