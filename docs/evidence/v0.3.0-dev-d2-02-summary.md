# v0.3.0-dev D2-02 证据摘要

## 本次范围

D2-02 本次完成的是 capability registry ceiling 与首个内置 capability 契约切片：

- 扩展 `CapabilityRegistryEntry`，使 capability 自身携带 read/write roots、network targets/methods、env keys、process targets、timeout 与 default effect。
- 让 `contract_digest` 覆盖新增 ceiling 字段，避免 grant 或 Action 绕过 registry truth。
- 新增 `python.unittest.locked` 内置 registry entry，固定 capability id/version/digest 与冻结 test id 白名单。
- 在授权检查中加入 registry ceiling fail-closed reasons。
- 更新 OpenAPI snapshot、生成的 TypeScript client、证据 manifest。

## 共同审查结论

- D2-02 不进入真实 executor：ACCEPT。
- Capability registry 必须加入 execution envelope ceiling：ACCEPT。
- `python.unittest.locked` 作为首个冻结 dev capability：ACCEPT。
- process/root 最终表达已在 D2-03a 收敛：registry ceiling + Action material + Grant 精确 subset；Claude 最终 F9/F10 ACCEPT。
- Approval consumption 已在 D2-03b 同事务实现并覆盖 replay 拒绝。

## 验证记录

- `python -m unittest tests.test_vnext_contracts -v`：通过。
- `python -m compileall nana_sidecar tests scripts`：通过。
- `npm run generate:client`：通过。
- `npm run check`：通过。
- `python -m unittest discover -s tests -v`：`216/216` 通过。

已知非阻塞现象：完整 unittest 结束时仍出现既有 UI shutdown `gc` ResourceWarning；未发现与 D2-02 变更相关的新失败。
