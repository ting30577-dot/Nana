# v0.3.0-dev D3-06 local completion evidence

Date: 2026-08-09  
Scope: exact fixture-only T2 `python.unittest.locked` journey bridge.

## Current decision

- Codex local implementation exit: **ACCEPT** after reopening and repair.
- Independent Claude implementation exit: **pending**.
- Joint D3-06 exit: **unresolved**.
- D3-07 implementation: **not authorized**.

The historical local ACCEPT was withdrawn by
`docs/d3_06_third_scan_findings.md`. The current local verdict is supported by
`docs/d3_06_reopening_batch_repairs.md` and a new no-edit scan in
`docs/d3_06_final_scan.md`.

## Implemented proof points

- browser StartRun cannot select backend/capability/test/grant/effects;
- real D1 args and result Artifacts with verified content-addressed bytes;
- append-only spawn fence without Run snapshot mutation;
- owner-lane D2 admission/claim/completion with no worker SQLite handle;
- one terminal transaction for result Artifact visibility, producer/Relation,
  Receipt, budget settlement, Gate-A..H audit, Action and Run terminal facts;
- deterministic restart convergence for pre-claim, pre-spawn, post-fence,
  cancel-pending and historical split-terminal windows;
- explicit conservative billing projection for uncertain effects;
- stream-reader and Windows Job close failure feed `termination_failed`.

## Verification record

| Verification | Result |
|---|---|
| `python -m compileall -q nana_sidecar tests scripts` | pass |
| `python -m unittest tests.test_d3_06_journey_runtime -q` | 17 pass |
| D1 Artifact commit/failure/reconciliation + D3-06 | 51 pass |
| D3 strict `-W error::ResourceWarning` subset | 107 pass, 2 platform skips |
| D2 strict `-W error::ResourceWarning` subset | 55 pass |
| full `python -m unittest` | 372 pass, 2 platform skips |
| `npm.cmd run check` | pass |
| `npm.cmd test` | 23 Vitest pass + projection self-test pass |
| `npm.cmd run build` | pass |
| D0 manifest verification within full suite | pass |

The full suite still prints the previously attributed PySide6 shutdown
`ResourceWarning` after an otherwise successful exit. It is absent when the
D2 and D3 handle/process/write suites promote ResourceWarning to an error.

## External review status

The current sanitized packet is
`docs/claude_d3_06_implementation_exit_packet_sanitized.md`. Its privacy scan
found no absolute path, configured endpoint, credential, environment value or
user/machine identifier. The product owner has already authorized this exact
sanitized packet to be sent through Nana's configured Claude-compatible relay.
The ordinary adapter attempts failed before review because the relay connection
was denied at the local socket/network layer. No official Anthropic endpoint,
OAuth flow, or alternate disclosure path is permitted, and no additional user
authorization is required. No external verdict has been received.

## Current Codex-only closure — 2026-08-11

The preceding external-review section is retained only as historical evidence.
Under `docs/d3_codex_only_governance_decision_20260811.md`, Claude is no longer
a prerequisite and its unavailable verdict is not represented as ACCEPT or
VETO. Codex independently re-audited the live worktree, opened and repaired
F-33 through F-35, and issued the current exit in
`docs/d3_06_codex_independent_exit_20260811.md`.

Current verification: 19 focused D3-06 tests; 67 D1 Artifact plus D3-06 tests;
125 strict D3/pre-gate tests with 2 platform skips; 388 full Python tests with
2 platform skips; compileall and TypeScript check pass. The exact T2-only
boundary is unchanged.

**Current D3-06 status: Codex-only ACCEPT.** This is not D3-07 capability or
filesystem-write authorization.
