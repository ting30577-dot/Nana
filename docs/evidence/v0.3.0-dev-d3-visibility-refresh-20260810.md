# v0.3.0-dev D3 visibility-refresh evidence — 2026-08-10

The read-only browser bridge now exposes a canonical `refresh()` operation.
When the document becomes visible again, the React surface aborts the prior
stream generation, rebuilds the bootstrap snapshot, and opens a new
authenticated SSE stream from that snapshot's high-water cursor. Terminal
projection/session failures remain terminal and cannot be bypassed by a local
refresh.

Verification:

- Store unit tests cover refreshed bootstrap/stream ordering and terminal
  refresh refusal and recovery-budget preservation; Vitest: 58 tests passed
  plus the projection self-test.
- A real-browser test dispatches `visibilitychange` and observes one new
  bootstrap followed by one new stream request; the full read-only E2E suite
  passes 17/17.
- TypeScript strict check and production Vite build passed.

This is still read-only synchronization. No mutation route, authorization
derivation, capability registration, export, or filesystem write path changed;
the D3-07 machine gate remains unresolved with all write flags false.
