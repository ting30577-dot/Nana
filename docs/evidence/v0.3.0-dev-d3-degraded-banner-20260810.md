# v0.3.0-dev D3 degraded-projection banner evidence — 2026-08-10

The read-only Cockpit now exposes the frozen D3-03 unknown-event semantics:
an unknown canonical event remains visible in Activity, the projection is
marked `degraded`, and the UI explicitly says that no domain state was
inferred and a newer launcher session is required. No authorization, success,
retry, or mutation state is invented.

The quarantine rule also treats a canonical Receipt with `result=
effect_unknown` as sufficient to show the no-retry quarantine, even if a
malformed or partial snapshot did not mirror that state onto Run or Action.
The Failed / uncertain metric counts literal negative Run, Action, and Receipt
facts, including `timed_out`, without rewriting any state.

Verification:

- `npm.cmd run test`: 25 Vitest tests passed plus projection self-test.
- `npm.cmd run check`: TypeScript strict check passed.
- `npm.cmd run test:e2e`: 13 Playwright tests passed, including the new
  unknown-event/degraded-banner and Receipt-only quarantine cases; production
  Vite build passed.
- The D3-07 machine gate remains unresolved with all write flags false.
