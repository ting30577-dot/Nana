# D3-05 completion evidence (local repair scan)

Date: 2026-08-08

## Scope

D3-05 is the authenticated, closed-union dev journey mutation surface over the
D2 canonical facts. D3-06 and later remain excluded.

## Local verification

- `python -m compileall nana_sidecar tests scripts`: pass.
- `python -W error::ResourceWarning -m unittest tests.test_d3_workspace_lock tests.test_d3_runtime_authority tests.test_d3_read_models tests.test_d3_journey_commands tests.test_d3_journey_runtime tests.test_claude_reviewer -q`: 90 tests, 2 platform skips, pass.
- Full Python unittest: 353 tests, 2 skips; one historical shutdown GC
  ResourceWarning remains outside the strict D3 subset. The only assertion
  failure before evidence synchronization was the stale D0 manifest digest;
  the 102-entry D0 manifest is now synchronized.
- `npm run check`: pass.
- Vitest/projection self-test: 23 tests pass.
- Vite production build: pass.
- Playwright read-only cockpit E2E: 11 tests pass.
- Claude adapter compatibility: rate-limit fallback is covered by
  `tests.test_claude_reviewer` and does not alter the D3 runtime contract.

## Repair coverage

The first frozen scan findings F-01 through F-17 are repaired locally. Tests
cover Workspace owner-thread close, failed-control executor cleanup, actor and
structured-error replay binding, aggregate/payload/domain binding, valid
Evidence relation state, portable and symlink resource guards, OpenAPI error
responses, duplicate route multiplicity, canonical schema constants, stable
actor identity, bootstrap fact integrity, active-edge races, duplicate HTTP
headers, handler before/after commit faults, committed SSE outbox visibility,
and HTTP response-loss replay.

F-18 is now attributed to the pre-existing PySide6/legacy UI teardown graph;
see `docs/evidence/d3_05_resourcewarning_attribution.md`. It remains a
non-blocking legacy caveat, while the strict D3 warning gate is clean. F-19 is
synchronized by the D0 manifest, this completion record, and the D3-05
manifest.

## Decision state

Codex local scan: **ACCEPT for implementation repair completion**.

Claude clean current-packet exit: **joint ACCEPT** for F-01 through F-19 and
D3-05. The final packet includes twenty barrier-synchronized active-edge
rounds, exact error assertions, the F-18 attribution, and current manifests.
D3-06 is now open for its own design/review gate, not inherited as accepted.
