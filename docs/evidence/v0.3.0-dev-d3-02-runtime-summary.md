# D3-02 runtime authority evidence summary

Date: 2026-08-01
Status: jointly ACCEPTED for read-only runtime authority

Joint exit decision: Codex `ACCEPT`; Claude `ACCEPT`.

## Scope

D3-02 creates the sole served runtime/OpenAPI authority. It remains read-only:
no mutation method, external export, Approval flow, or React UI is enabled.

## Evidence

- WorkspaceRuntime is the only source of the event-stream database path.
- Lifespan performs D3-01 start/reconciliation before `ready`; failed startup
  re-raises after cleanup, and a replacement owner can start.
- Only `GET /healthz` and qualifying exact preflight are public. Health exposes
  only state. All runtime reads require one Bearer token, one exact
  `http://127.0.0.1:<port>` Origin, and matching Host.
- Slash redirects and docs UI are disabled; forwarded/duplicate/invalid security
  headers fail closed. No wildcard CORS header exists.
- SSE drain is bounded. Stream cleanup completes before Workspace close, and a
  forced timeout cancels the stream before SQLite/lock shutdown.
- Runtime OpenAPI, generated TypeScript, and the checked-in web snapshot share
  one runtime source. The frozen D0 OpenAPI fixture is retained and checked as
  a compatible path/schema subset.
- A real loopback process starts, serves authenticated reads, is killed, and a
  same-Workspace restart becomes ready again.

## Candidate verification

- focused D1+D3 HTTP/runtime suites: 24 passed
- full Python suite: 293 passed, 1 existing environment skip
- compileall: pass
- TypeScript check: pass
- D0 and D2 102-entry manifests: 0 mismatches; digest
  `d7254aa074b503b5a404d1bdaaa16478e6fc87c9d88393fa0f9f00380285d02b`

## Known boundary

The full suite still emits the pre-existing PySide6 shutdown uncollectable
object diagnostic after successful completion. D3-02 focused runtime tests are
strict-warning clean. This does not authorize mutation serving; its relation to
future process/handle/write paths must be established before that gate.

Claude additionally recommends replacing the current D0 path/schema subset
check with an explicit extra-route allow-list before a later stage adds new
methods, and preserving failed-injection assertion output in future evidence.
