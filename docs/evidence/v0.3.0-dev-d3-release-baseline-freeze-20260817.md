# v0.3.0-dev D3 release-baseline freeze

Date: 2026-08-17

Status: `acceptance_complete_release_baseline_frozen`

## Commit binding

- Observation/result commit: `278f3a4186d7d0f85f6caf715c2882d63c589fc6`.
- Binding rule: the release-baseline commit is the direct successor of that
  result commit and contains the evidence/authority freeze plus regenerated
  manifest files.
- The final manifest and detached SHA-256 are the canonical content binding;
  this record intentionally does not embed their digest, avoiding a circular
  manifest self-reference.

## Frozen verification

- `python -m compileall nana_sidecar tests scripts`: PASS.
- Full Python unittest discovery: 446 run, 446 passed, 0 skipped.
- TypeScript no-emit check: PASS.
- Vitest: 67/67; projection self-test: PASS.
- Vite production build: PASS.
- Browser release matrix: 27/27, Playwright retries=0.
- Ten consecutive full success journeys: 10/10, Playwright retries=0.

The full Python process emitted the already-documented PySide6 shutdown
`ResourceWarning` after the successful test result. The warning is reproducible
from the frozen legacy PySide6 stack; strict D3 handle/process/write paths have
separate ResourceWarning-as-error coverage. It is not suppressed and is not
reported as a D3 runtime failure.

## Scope retained

This freeze does not expand the D2 locked unittest claim into a hostile-code
sandbox, does not authorize arbitrary shell/Python or remote publish, and does
not reinterpret the missing observation measurements as measured rubric
success or subjective satisfaction.
