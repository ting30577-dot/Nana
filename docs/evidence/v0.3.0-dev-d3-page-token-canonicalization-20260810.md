# v0.3.0-dev D3 page-token canonicalization evidence — 2026-08-10

Signed bootstrap section tokens now fail closed on non-canonical Base64URL
characters, alternate encodings, extra separators, and non-string inputs.
The decoder still verifies the HMAC before accepting the bounded high-water,
section, offset, and watermark payload. This tightens input handling without
changing the signed token shape or section protocol.

Verification:

- D3 read-model tests cover repeated valid decoding, tampering, invalid
  alphabet/trailing bytes, extra separators, and boolean/integer bounds.
- The D3 strict suite and full Python baseline remain green.
- No route, mutation, authorization, capability, export, or filesystem write
  behavior changed; D3-07 remains machine-gated.
