# v0.3.0-dev D2-03b 证据摘要

D2-03b 本次完成本地 Capability admission service 切片：

- 新增 `nana_sidecar.storage.admission.CapabilityAdmissionService`；
- policy grant hit 原子完成 Action authorized、`action.authorized` Event、outbox append、grant use consumption；
- grant last use 会把 grant 标记为 `exhausted`；
- policy grant miss 不改变 canonical state；
- approval hit 原子完成 Action authorized、Event/outbox 与 `approval_consumptions`；
- approval replay / 已有 consumption fail closed；
- admission 从 args artifact 字节重建 canonical args，并校验 artifact blob hash、args hash、action hash、capability digest、requested effects 与 risk tier；
- outbox fault injection 证明 action/event/grant use 在同一事务回滚；
- 双连接二次授权同一 Action 失败，未产生第二个 event。

新增/变更文件：

- `nana_sidecar/storage/admission.py`
- `nana_sidecar/storage/__init__.py`
- `tests/test_d2_capability_admission.py`
- `docs/claude_d2_03b_design_packet_sanitized.md`
- `docs/d2_03b_decision_record.md`
- `docs/evidence/v0.3.0-dev-d2-03b-summary.md`

验证：

- `python -m compileall nana_sidecar tests scripts`：通过。
- `python -m unittest tests.test_d2_capability_admission -v`：8 tests OK。
- `python -m unittest tests.test_d2_capability_admission tests.test_d2_run_scheduler tests.test_vnext_storage tests.test_vnext_contracts -v`：71 tests OK。

Claude 最终协作状态：二次收敛复审确认 F3 ACCEPT；持久事实派生、原子 consumption 与 durable authorization material 已共同收敛。
