---
title: Nana 首个纵向切片执行清单
status: implementation-gate
date: 2026-07-29
updated: 2026-08-13
milestone: v0.3.0-dev
tags:
  - nana
  - execution
  - checklist
---

# Nana 首个纵向切片执行清单

> [!important] 当前 D3 权威（2026-08-13）
> 唯一当前实现与验证权威为 `C:\Users\q1968\Desktop\Nana\docs\CURRENT_D3_AUTHORITY.md`。
> 本文中与其冲突的旧 gate、重开说明和未勾选项只作为历史快照保留。

## 1. 目标

不用先实现完整 Nana，证明一个真实算法 Inquiry 能在新架构中完成：

`创建 → 最小 Resource/Locator → 可编辑 Plan → 注册工具 Run → Event/Activity →
Artifact → Finding draft → 审批后导出测试报告`

并能取消、失败、重启和恢复，不损坏数据。

## 2. 实施前用户门

> [!warning] 必须确认
> 开始业务实现前，由用户确认首个真实算法问题。AI 可以推荐，不能替用户决定。

推荐候选：

> 为什么可变长度滑动窗口通常依赖非负输入？引入负数后单调性在哪里失效，
> “和至少为 K 的最短子数组”如何用前缀和与单调队列解决？

它适合作为首切片，因为：

- 当前仓库已有滑动窗口资产；
- 能产生明确 Hypothesis 和最小反例；
- 可比较朴素、错误滑窗和正确单调队列；
- 计算成本低；
- dev 可验证最小代码、测试、Run、Evidence 和 Finding draft；alpha.1 再验证完整
  反例、三种实现、benchmark 和 Decision；
- 不需要先承担 PDF parser 风险。

用户也可选择自己的真实算法问题，但必须有清楚的成功标准。

> [!success] 已确认
> 2026-07-29 用户选择推荐的滑动窗口/负数问题。dev 只运行冻结的现有非负输入
> 单元测试并产出可追溯 Finding draft；反例、单调队列实现、三实现比较、
> property test、benchmark 与最终 Decision 仍全部后置到 alpha.1。

## 3. Workstream 0：冻结与基线

### 任务

- [x] 标记 `v0.2.0-alpha` 为 legacy prototype；
- [x] 禁止旧 PySide6 新功能；
- [x] 保存 UI 截图与当前 schema inventory；
- [x] 运行并记录现有测试：2026-07-29，61/61 通过；
- [x] 建立 ADR 索引；
- [x] 记录哪些纯算法/测试/Claude adapter 可复用。

### DoD

- 当前行为有可重放基线；
- 没有新旧 UI 双写计划；
- 任何删除/迁移都能回到备份。

## 4. Workstream 1：契约先行

### ADR

- [x] ADR-001：Project Execution 主循环；
- [x] ADR-002：React/TS + FastAPI，Tauri 后置 spike；
- [x] ADR-003：SQLite canonical + Artifact Store；
- [x] ADR-004：Command/Action/Event/PolicyGrant/Approval/Receipt；
- [x] ADR-005：Capability Registry；
- [x] ADR-006：数据分级和模型路由；
- [x] ADR-007：ExecutionBackend；
- [x] ADR-008：旧数据只读迁移。

### Schema

- [x] Project；
- [x] Inquiry；
- [x] Plan + revision；
- [x] Run + immutable snapshot；
- [x] Action + action hash/state；
- [x] Event + aggregate/version/actor/run_seq；
- [x] PolicyGrant/Approval/Receipt；
- [x] Artifact + blob hash；
- [x] Resource/Locator；
- [x] Claim/Evidence/Hypothesis/Finding draft；
- [x] typed Relation registry；
- [x] migration/version/schema ceiling。

`schema_version=1`、`schema_read_ceiling=1` 与 migration `0001_contract_kernel`
已实现。dry-run 不创建目录/文件；初始化与重复打开幂等；单步失败整步回滚；高于读取
上限、迁移 hash 被篡改、非空未版本化数据库均在继续写入前拒绝。这里完成的是 D0
schema 迁移契约，不代表 legacy 数据迁移或 D1 Artifact/Event runtime 已实现。

### DoD

- schema、state machine、command 和 event 有机器可测试契约；
- Python 与 TypeScript 类型由同一 OpenAPI/JSON Schema 生成；
- 无前端直接写数据库。

## 5. Workstream 2：Python sidecar

### 任务

- [x] FastAPI app factory；
- [x] loopback/random port 与启动 token；
- [x] health/version/schema handshake；
- [x] command idempotency；
- [x] SQLite transaction + outbox；
- [x] SSE replay；
- [x] Artifact stage/hash/commit/reconciliation protocol；
- [x] local scheduler；
- [x] pause/cancel/timeout/orphan recovery；
- [x] structured error model。

D0 已冻结并生成 `ErrorResponse`/`StructuredError` 类型；D1/D2/D3 已将受支持 runtime
失败映射为失败关闭的结构化 envelope，并由当前回归覆盖。

### DoD

- 进程重启后 UI 可从 event id 补齐；
- 重复 Command 不重复副作用；
- 任意 Artifact 写入失败不向读取者暴露 unavailable blob，启动重扫确定性收敛；
- Run 终态不可回退。

## 6. Workstream 3：Capability / Policy / Budget

### 首批注册工具

- [ ] 读取项目内文件；
- [ ] 写 scratch Artifact；
- [x] 运行锁定的 Python 测试；
- [ ] 运行锁定的 benchmark；
- [ ] Git 只读 status/diff/commit ref；
- [ ] 公共网页读取（可在 alpha.1 完整化）。

### 任务

- [x] Capability JSON Schema；
- [x] 路径真实解析与 allow-root；
- [x] 网络策略；
- [x] 最小环境变量；
- [x] 进程树/超时/输出限制；
- [x] Run Budget；
- [x] PolicyGrant constraints/revoke/expire；
- [x] Approval action hash；
- [x] Receipt；
- [x] T3 fixture：审批后向用户选择的 Workspace 外测试目录导出无敏感 draft；
- [x] policy violation 测试。

### DoD

- Agent 无法调用未注册工具；
- 参数/路径越界在执行前被拒；
- 取消停止新 Action；
- 预算达到 100% 不再启动动作；
- 锁定安全语料中未批准的 T3/T4 动作为零；
- Grant 只覆盖满足 capability/参数/目录/网络/预算/期限约束的 Action。

## 7. Workstream 4：React 纵向 UI

### 最小 Cockpit

- [ ] Capture；
- [ ] Active Project；
- [ ] Needs You；
- [ ] Running/Failed。

### 最小 Studio

- [ ] Project tree；
- [ ] Inquiry/Plan editor；
- [ ] Markdown/Code tab；
- [ ] AI Plan/Activity；
- [ ] Run Log/Tests；
- [ ] Approval card；
- [ ] Artifact/Finding draft。

### DoD

- 用户不需要理解数据库对象就能完成旅程；
- 状态从 canonical projection 派生；
- 页面刷新不丢已提交状态；
- keyboard path、Windows 125%/150% DPI 通过；
- 失败、取消、等待审批有清楚反馈。

## 8. Workstream 5：dev 最小真实算法 fixture

### Plan

- [x] 明确问题、输入范围、验收标准；
- [x] 注册一份仓库内已有的公开 Markdown/代码 Resource；
- [x] 为一个明确片段创建 locator、Claim 和 Evidence；
- [x] 定义一个待验证 Hypothesis；
- [x] 计划运行一个锁定的现有测试；
- [x] 冻结预算与审批点。

### Runs

- [x] 运行一个锁定的 Python 单元测试；
- [x] 保存输入、代码版本、环境摘要、stdout/stderr 和退出状态；
- [x] 失败/取消演练。

### Artifacts

- [x] 问题说明；
- [x] 最小 Evidence 与 locator；
- [x] test result Artifact；
- [x] Finding draft；
- [x] 无敏感内容的 draft report；
- [x] T3 导出 Approval 与 Receipt。

### DoD

- Finding draft 可回到 Evidence 或 Run；
- Resource/locator/relation 通过最小契约；
- 测试代码/输入/环境/结果完整；
- 用户能修改 Plan；
- 取消/失败/重启恢复通过；
- 外部测试目录只有审批后才出现导出；
- 不生成 benchmark、完整反例研究或最终 Decision。

### alpha.1 明确后置

错误滑窗反例、朴素/滑窗/单调队列三种实现、性质测试、benchmark、完整 Finding
review 和用户确认 Decision 全部属于 `v0.3.0-alpha.1`，不得回流到 dev。

## 9. Post-dev 架构门：Tauri spike

仅在 Web dev 旅程通过并正式结束后开始；它不是 dev DoD。

- [ ] Tauri 2 shell；
- [ ] sidecar 启动/健康/退出；
- [ ] 随机端口和启动 token；
- [ ] 单实例/多窗口策略；
- [ ] crash/orphan 恢复；
- [ ] Windows 打包；
- [ ] 升级/卸载保留数据；
- [ ] DPI、文件选择、深链。

### Gate

- 全部量化门槛通过：进入正式桌面迁移；
- 失败但 Web 旅程优秀且浏览器安全门通过：选择本地 Web Plan B；
- 两者都不稳定：停下重评，不回旧 Qt 堆功能。

## 10. 测试清单

### Contract

- [x] schema constraints；
- [x] state transitions；
- [x] relation types；
- [x] Resource/Locator/Evidence/Finding minimum cardinality；
- [x] Action hash / PolicyGrant matching / Approval invalidation；
- [x] aggregate Event actor/version/run_seq；
- [x] idempotency；
- [ ] budget/policy；
- [x] locator serialize。

### Failure injection

- [x] DB commit 前后崩溃；
- [x] blob 写半；
- [x] SSE 断线；
- [ ] child timeout；
- [ ] cancel race；
- [ ] Provider 不可用；
- [ ] 磁盘空间不足；
- [ ] Approval 过期/内容变化。

### Security

- [ ] `..`、junction/symlink 路径逃逸；
- [ ] shell metacharacter；
- [ ] 未授权网络；
- [ ] 子进程密钥泄露；
- [ ] prompt injection；
- [ ] 超大输出；
- [ ] Action replay。

数量与阈值直接采用 [[05_技术架构与数据契约#14. 架构决策门]] 和
[[06_AI自治_安全与隐私#13. 安全测试门槛]]；测试报告必须记录 corpus version、
seed 和 trace，不能只写“通过”。

### UI E2E

- [x] create → plan → run → approve → artifact；
- [x] pause/resume；
- [x] cancel；
- [x] failed → retry_of；
- [x] reload/reconnect；
- [x] keyboard/DPI。

若采用 Local Web Plan B，另加：

- [x] Host/Origin/CORS/CSRF；
- [x] bootstrap token 单次交换与重放；
- [x] 恶意本地网页；
- [x] browser reload/close 与 active Run 生命周期；
- [x] 第二 launcher/workspace lock。

## 11. 推荐任务顺序

| 顺序 | 任务 | 依赖 | 退出证据 |
|---:|---|---|---|
| 1 | 冻结/基线/ADR | 无 | inventory + tests |
| 2 | schema/state machine | 1 | contract tests |
| 3 | Artifact commit/reconcile | 2 | 每崩溃窗口 20 次 fault injection |
| 4 | Command/outbox/SSE | 2 | replay/idempotency |
| 5 | scheduler/cancel | 3–4 | process tests |
| 6 | capability/policy/budget | 5 | 锁定语料 zero unauthorized |
| 7 | minimal React UI | 2–6 | UI E2E |
| 8 | dev 算法 fixture | 7 + 用户问题确认 | Finding draft + export Receipt |
| 9 | dev gate review | 1–8 | go / rework / stop |
| 10 | post-dev Tauri spike | dev 通过 | Tauri / secure Web / stop |

## 12. 每次开发会话的最小闭环

无固定工时时，每次只拿一个 30–120 分钟可完成单元：

1. 从当前 milestone 选择最小失败测试；
2. 写清预期 Artifact/证据；
3. 实现到测试通过；
4. 更新 ADR/contract（若语义变化）；
5. 记录未解决风险；
6. 不顺手加入下一 milestone 功能。

## 13. 开工前最后选择

实现阶段需要用户确认：

1. 使用推荐的滑动窗口/负数问题；
2. 选择另一个真实算法问题；
3. 先只做 runtime，不进入算法内容（不推荐，因为会削弱真实旅程证据）。

这项选择不阻塞本次规划与 Vault 重建；它只阻塞 `v0.3.0-dev` 业务实现。

## 14. 当前实施状态

### 2026-07-29：D0-03 交接模拟后完成

D0-01 的第一次记录只证明了骨架可生成，遗漏了完整授权匹配、全部 locator、
Relation/cardinality 约束、迁移失败回滚和完整证据 manifest，因此不能作为 D0
最终完成证明。D0-02 随后的 D1 交接模拟又发现两个缺口：`staged` Artifact 可缺少
`temp_ref`，以及 `artifact.committed` 可携带空 payload。D0-03 已补齐并重新验证：

- Workstream 0 冻结、legacy schema/UI/reuse 基线与 ADR-001 至 ADR-008 完成；
- 22 个 dev Command、46 个封闭 Event type、17 组状态机、12 个 Relation type、
  6 类可重开 Locator 和结构化错误 envelope 均有机器契约；
- `artifact.staged / committed / reconciled` 具有严格 payload 与可判别
  `ArtifactLifecycleEvent` 联合类型；空 payload、aggregate id 不一致、路径逃逸和
  非法 reconciliation transition 在写入前拒绝；
- `ActionHashMaterial` 覆盖规范化参数、Capability/version/digest、数据级别、
  Provider、读写/网络/进程副作用、网络方法、预算、风险与可逆性；任一变化使旧
  Approval 失效；
- PolicyGrant 精确匹配 Project、Capability、参数 schema、安全级别、Provider、
  目录、网络、预算、并发、次数和期限；未知 schema keyword 失败关闭；
  `decision.confirm / export.publish / object.delete` 不能由 Grant 预授权；
- schema v1 的 24 张 canonical 表由 migration `0001_contract_kernel` 建立；
  dry-run、幂等打开、失败整步回滚、迁移 hash、读取上限和 legacy 隔离均已测试；
- `staged` Artifact 必须携带非空逻辑 `temp_ref`，`available` Artifact 不得保留
  `temp_ref`；SQLite 对三类 Artifact 生命周期 Event 再做失败关闭校验；
- D0→D1 metadata 模拟覆盖 staged/Event/outbox 原子提交、提交前崩溃回滚、重开、
  available 可见性与 10,000 Event cursor replay，2/2 通过；
- 同一 OpenAPI 生成 108 个 schema 与 TypeScript 类型，严格类型检查通过；
- 用户选择的滑动窗口/负数问题、公开 Resource/Locator hash、dev 成功标准和
  alpha.1 scope ceiling 保持冻结，没有偷渡反例、三实现、property、benchmark
  或最终 Decision；
- focused D0 测试 58/58、全量测试 119/119 通过；49 文件证据 manifest 可重算；
- sidecar 仍只有 3 个只读路径并返回 `mutations_enabled=false`，没有把 D0
  契约完成写成 Runtime 完成，也没有新增旧 PySide6 功能。

当前没有已知 D0→D1 契约阻断。下一项是 D1 Artifact/Event runtime：在已冻结
payload、schema 与 handoff 测试上，以失败测试实现真实 `.partial`/fsync/hash/
同卷 rename/reconciliation，再做 command transaction/outbox dispatcher/HTTP
SSE；在这些 gate 通过前不建设 React 业务界面。

### 2026-07-29：D1-01 Artifact 文件系统失败边界完成

从冻结 commit `03706e30fe7244552793dabe1c1be9d1e75e40b6` 创建
`codex/v0.3.0-dev-d1-runtime`，保留用户已有工作树修改且未修改/amend D0
checkpoint。D1 第一条真实 runtime 证据来自 `.partial` 与故障注入：

- [x] blob 写半后只留下 Workspace 内 `.partial`，最终 blob 对读取者不可见；
- [x] flush 失败与 flush 成功后 fsync 失败均不 promotion；
- [x] staged 文件 hash 或 size 与声明不一致时拒绝；
- [x] 同 hash 最终 blob 已存在但损坏时拒绝复用，并保留 `.partial`；
- [x] atomic rename 失败时保留 `.partial`，不出现最终 blob；
- [x] 跨卷输入先复制到 Workspace 同卷 staging，再从 staging rename；
- [x] `.partial` 与 canonical state=`staged` 即使最终路径已存在也不能读取。

本工程单元只建立候选布局
`artifacts/.staging/*.partial → artifacts/sha256/ab/cd/<full-hash>` 的文件系统
原语和可注入故障边界。它没有建立 Artifact staged/Event/outbox 数据库事务，
没有把 Artifact 标为 `available`，也没有实现 reconciliation、Command 或 HTTP
SSE；因此 Workstream 2 的 Artifact protocol 总项仍不勾选。下一工程单元为
D1-02 Artifact stage/hash。

#### D1-01 完整审查补正

完成首次实现后的逐项复审发现并修复 3 项：

1. rename 成功但 canonical state 仍为 `staged` 时，进程内列表曾提前把 blob
   视为 available；现已删除该隐式 promotion 状态，可见性列表必须显式接收
   canonical state，`staged` 在最终文件已存在时仍返回零；
2. 原测试只注入 fsync 失败，未独立注入 flush 失败；现已拆为两个故障测试；
3. 原“写半”fixture 实际写入 4/16 字节，且故障位置缺少边界校验；现已锁定严格
   半写，并在创建 `.partial` 前拒绝负数或写满位置。

补正后 D1-01 聚焦测试为 9/9，全量回归为 128/128；D0 manifest 仍为 49/49。
本次补正没有进入 D1-02，也没有改变 Artifact CANDIDATE 目录、schema、架构或范围。

### 2026-07-29：D1-02 Artifact stage/hash 完成

D1-02 在 D1-01 文件系统故障骨架上建立可交给 D1-03 的完整 stage 描述符，仍不写
SQLite：

- [x] `.partial` 完成 flush/fsync 后才重新读取并计算 SHA-256 与实际 size；
- [x] stage 结果包含 Workspace 相对、正斜杠形式的 `temp_ref`，并可直接通过冻结
  `ArtifactStagedPayload` 契约；
- [x] media type 必须是最长 160 字符的规范 `type/subtype` token；
- [x] 文件导入使用进程内冻结的 Python 标准 MIME 文件名映射计算 media type，
  不读取宿主 Windows 注册表覆盖，并在创建 partial 前拒绝与声明值不一致的输入；
- [x] `verify_staged` 同时校验 partial 路径、`temp_ref`、hash、size 和 media type，
  伪造任一 stage 描述字段均拒绝；
- [x] 冻结的首个算法 Markdown Resource 实际 stage 后 hash 为
  `sha256:437fbf2320eacc59e01adc16503f5297383551b77c2457b1a054bcf4b7e5fe4a`，
  size 与源文件一致，media type 为 `text/markdown`；
- [x] stage 后只有 `.partial`、没有最终 blob，canonical state=`staged` 的单读与
  列表读取均不可见；
- [x] 已有最终 blob 只有在 hash/size 复验通过后才复用；合法复用不再 rename，
  损坏 blob 仍由 D1-01 测试拒绝。

D1-02 复审发现初版 `verify_staged` 只复测文件、未证明 stage 描述符字段彼此自洽；
补充红测后已修复；最终复审又发现模块级 MIME 映射可能受 Windows 注册表影响，现已
换成进程内冻结的标准映射并加入宿主覆盖反例。media type 当前证据只证明确定性的
文件名 MIME 映射和声明一致性，不声称通用内容嗅探。Artifact Store 总项仍不勾选，
因为 staged/Event/outbox 原子事务与 available 第二事务属于 D1-03。

#### D1-02 第二次完整审查补正

按 `.partial` 不可见、hash/size/media type 校验和已有 blob 验证复用逐项重新扫描，
先完成全部扫描再修复，发现并关闭 3 项：

1. 正常写入路径原先忽略底层 `write()` 的实际返回长度，短写可能被重新 hash 后
   当成完整 stage；现改为循环写满，零进展或非法返回长度立即失败并只留不可见
   `.partial`；
2. 已有最终路径为目录时原先泄漏 `PermissionError`，符号链接可能被跟随；现要求
   最终 blob 必须是非链接正规文件，否则统一作为 `ArtifactIntegrityError` 拒绝，
   并保留 staged partial；
3. 默认同卷判定原先只比较 Windows 盘符，不能识别同盘符挂载卷/junction；现改为
   最近已存在路径的 `st_dev` 设备标识，rename 前比较真实卷身份。

补正后 D1-02 为 13 个 test method；D1-01 + D1-02 聚焦 22/22，全量回归
141/141。复审没有进入 D1-03，没有修改 schema/API，也没有锁定 CANDIDATE 目录。

### 2026-07-29：D1-03 两阶段 commit 完成

D1-03 直接消费 D1-02 的 `StagedArtifact`，不重新计算或复制另一套 metadata：

- [x] 第一 SQLite `BEGIN IMMEDIATE` 事务原子写 Artifact state=`staged`、
  `artifact.staged` Event 与 outbox；Event aggregate version 为 1；
- [x] 任一 INSERT 或最终 SQLite COMMIT 失败时三者全部 rollback，连接不遗留活动
  transaction，Workspace partial 保持不可见并交给 D1-04；
- [x] 第一事务提交后才执行 D1-01/D1-02 验证过的同卷 `os.replace`；
- [x] rename 失败时第一事务保持 staged、partial 保留、只有 staged Event/outbox；
- [x] rename 成功后、第二事务前崩溃时为 staged + final，canonical reader 仍拒绝；
- [x] 第二事务持写锁后重新校验 canonical staged metadata 与最终 blob
  hash/size/正规文件身份，再原子写 state=`available`、清空 `temp_ref`、
  `artifact.committed` Event 与 outbox；Event aggregate version 为 2；
- [x] rename 后最终 blob 丢失或损坏时不进入 available，保留 staged 供 D1-04
  reconciliation；
- [x] 第二事务 Event/outbox 失败时 state update 一并 rollback，重开数据库后仍为
  staged + final 且不可读；
- [x] 成功路径重开数据库后只有 canonical state=`available` 可由
  `ArtifactReader` 读取，Event/outbox 精确为两条。

#### D1-03 阶段衔接审查

本阶段完成后按“全量扫描 → 集中列错 → 逐项修复 → 最终复审”检查：

- D1-02 → D1-03：portable `temp_ref`、hash、size、media type 直接进入冻结的
  `ArtifactStagedPayload` 和 schema v1，没有平行 metadata 模型；
- D1-03 → D1-04：确定留下 `partial only/无 DB`、`staged + partial`、
  `staged + final`、`available + final` 四类可判别形态；前三类均不可读；
- 初版复审发现 SQLite 最终 COMMIT 失败未显式 rollback，已用 deferred foreign
  key 红测修复；
- 阶段末扫描发现 rename 后 final 丢失/损坏仍可能被标 available，已增加第二事务
  内复验并修复。

D1-03 有 10 个 test method；D1-01 至 D1-03 聚焦 32/32，全量 151/151。
Artifact protocol 总项仍不勾选，因为规格 8.3 六分支 reconciliation 属于 D1-04。

### 2026-07-29：D1-04 Artifact reconciliation 完成

D1-04 只消费 D1-03 留下的 canonical DB state、`temp_ref` 与 content-addressed
hash path，按规格 8.3 的六个启动分支确定性收敛：

- [x] 无 DB 的过期 partial 原子移入 Workspace 同卷 orphan quarantine；grace
  period 内的新 partial 保留；
- [x] DB staged + 完整 partial 完成同卷 rename，再以 Artifact/Event/outbox
  单事务标 available；
- [x] DB staged + 已有最终 blob 复验 hash/size 后标 available；
- [x] DB staged + partial/final 都缺失或损坏时，以同事务标 failed，读取仍拒绝；
- [x] DB available + blob 缺失或损坏时，以同事务标 corrupt，不返回空内容或损坏
  内容；
- [x] 无 DB 的最终 hash blob 原子移入 orphan quarantine；
- [x] 六个分支各在 20 个全新 Workspace/SQLite fixture 注入崩溃并重开，共 120
  次；崩溃前后 unavailable reader 成功返回次数为零；
- [x] 每个 fixture 收敛后再次扫描均为零动作；事务内故障时 state、reconciled
  Event 与 outbox 一并 rollback。

#### D1-04 完整扫描、补正与最终复审

首次六分支实现通过后，先完成全量扫描并集中列出问题，再逐项补红测与修复：

1. 共享同一 hash 的 staged/available Artifact 曾按 ID 交错扫描，结果可能受 UUID
   排序影响；现先统一修复全部 staged blob，再校验 available，两个记录均确定
   收敛；
2. staged + 合法 final + 损坏/多余 partial 曾需第二次扫描才隔离残留；现首次扫描
   即移入 partial quarantine，再提交 available；
3. quarantine 卷判断曾跟随源文件/链接目标；现比较源目录项与 quarantine 目录的
   真实卷身份，并以 `lstat` 安全处理坏链接；
4. failed/corrupt 分支原测试未在注入崩溃后立即核对事务回滚；现逐次核对 canonical
   state、Event 与 outbox 数量，并在成功恢复后核对递增版本；
5. 补齐 grace period、quarantine rename 失败重试、共享 hash、损坏 final 修复、
   available 损坏内容和首轮残留清理证据。

最终 D1-04 为 12 个 test method；D1-01 至 D1-04 聚焦 44/44，全量 163/163，
Python compileall、TypeScript strict check 与 D0 49/49 manifest 均通过。最后复审
未发现剩余 D1-04 阻断。Artifact 目录仍为 CANDIDATE，没有 schema、架构、迁移或
D1 范围变化。下一工程单元为 D1-05 command transaction/idempotency/outbox。

### 2026-07-30：D1-05 command transaction/idempotency/outbox 完成

D1-05 以冻结的 `RevisePlan` Command 建立最小真实领域闭环；它不是 22 个 Command
处理器的批量实现，也没有新增 HTTP mutation endpoint：

- [x] canonical JSON + SHA-256 绑定 `command_id` 与完整 Command 内容；同 ID 同内容
  返回稳定 Event ID 与 revision，状态为 replayed，不重复写领域状态；
- [x] 同 ID 不同内容返回结构化 `E_COMMAND_REPLAY_CONFLICT`；
- [x] `expected_revision` 与最新 Plan revision 不一致、以及 Plan 不存在时返回并持久化
  结构化 `E_REVISION_CONFLICT`；
- [x] 新 Plan revision、`plan.revised` Event、outbox 与 accepted `command_log`/
  `CommandResult(affected_revisions,event_ids)` 在同一个 `BEGIN IMMEDIATE` SQLite
  事务提交；
- [x] outbox INSERT 失败与 SQLite 最终 COMMIT 失败均显式 rollback，领域 revision、
  Event、outbox、command log 不产生部分写入；
- [x] 提交前崩溃回滚后以同一 Command 重试只应用一次；提交后响应丢失时重放已提交
  结果，不丢 Event；rejected Command 的提交后响应丢失也稳定重放；
- [x] 两个独立 SQLite 连接以同一 `command_id` 竞争时，写锁内二次检查保证仅应用一次；
- [x] replay 前验证已存 `CommandResult` 与 command ID、Plan revision、Event causation
  和 outbox 行绑定；不一致时失败关闭。

#### D1-05 完整扫描、补正与最终复审

初版 7 个最小闭环测试通过后，先完成逐项扫描并集中列出 6 组证据缺口，再统一补红测：

1. 缺失 Plan 且 `expected_revision=None` 时，空值相等会进入写路径并泄漏 `TypeError`；
2. 已有 command 的快速 replay 绕过 idle SQLite connection 检查；
3. 只有语句级 outbox 失败，未覆盖 deferred foreign key 导致的最终 COMMIT 失败；
4. 只有顺序 replay，未证明两个 SQLite 连接竞争同一 ID 时只应用一次；
5. replay 直接信任 `result_json`，未校验其与领域 revision、Event、outbox 的绑定；
6. rejected Command 的提交后响应丢失，以及 JSON mapping 键顺序不影响 request hash，
   缺少显式证据。

补充到 14 个 test method 后，初版按预期出现 3 个 failure 与 1 个 error；逐项修复
缺失对象、连接空闲和 stored result 绑定后 14/14 通过，其余新增 COMMIT 故障、双连接
竞争、rejected replay 与 canonical hash 测试保持通过。最终 D1-01 至 D1-05 聚焦
58/58，全量 177/177；Python compileall、TypeScript strict check 与 D0 49/49
manifest 均通过。再次审查未发现剩余 D1-05 阻断。

本单元没有修改 schema/OpenAPI，没有建设 HTTP SSE、scheduler/cancel/process
runtime、Capability/policy/budget、React、Tauri 或 alpha.1 算法内容。Artifact
目录仍为 CANDIDATE，没有架构、迁移或 D1 范围变化。下一工程单元为 D1-06 HTTP SSE。

### 2026-07-30：D1-06 HTTP SSE 完成

D1-06 在新建的 D1 runtime app composition 中提供 `/api/v1/events`，冻结的 D0
`nana_sidecar/app.py` 与 checked-in D0 OpenAPI snapshot 保持逐字节不变：

- [x] `Last-Event-ID` 使用全局单调 Event ID；只接受 SQLite 范围内的非负十进制
  cursor，按 `event.id > cursor ORDER BY event.id` 有序补齐；
- [x] 同一 cursor 循环先 catch-up 后持续轮询 live；在“空扫描完成、进入等待前”插入
  Event 的确定性竞态中仍在下一轮返回，不产生缺口；
- [x] 只读取已提交且有 outbox 行的 canonical Event；未提交 Event+outbox 对流不可见；
- [x] 断线后以客户端最后确认的 Event ID 重连，补齐所有缺失 Event；稳定 ID 支持
  客户端去重，传输语义为至少一次，不承诺网络层 exactly-once；
- [x] 慢客户端每次只预取一个有界 batch，配置硬上限为 1024；HTTP disconnect
  取消 generator 并关闭该客户端的 SQLite connection；
- [x] SSE frame 包含稳定 `id`、typed `event` 与 canonical JSON `data`，响应使用
  `text/event-stream`、`no-cache, no-transform` 与 anti-buffering header；
- [x] session-protected API 与 SSE 共用同一个 `LocalSession` 校验；每次连接/重连
  重新验证 Bearer session 与精确 Origin；
- [x] session Origin 只接受带明确端口的 loopback HTTP(S) origin；缺失、错误或重复
  Authorization/Origin，以及重复 `Last-Event-ID` 均失败关闭；
- [x] Event stream 与 session 必须成对配置；runtime app 的 OpenAPI 实际包含
  `/api/v1/events`。

#### D1-06 完整扫描、补正与最终复审

首版 5 个最小闭环测试通过后，先完成全量扫描并集中列出 6 组问题，再统一补红测：

1. 重复 Authorization、Origin、Last-Event-ID 曾取第一项，存在安全/游标歧义；
2. session 配置曾接受外域、带路径、无端口或非 URI Origin；
3. session-protected API 与 SSE 共用会话缺少直接实现和证据；
4. 非法 cursor 分支与 catch-up/live 精确切换竞态证据不足；
5. batch size 没有硬上限，HTTP disconnect 释放连接缺少证据；
6. 未提交 Event+outbox 不可见及 SSE cache/anti-buffering header 缺少显式证据。

扩展到 11 个 test method 后按预期出现 10 个 failure；逐项修复后 11/11 通过，
最终再补 stream/session 成对失败关闭与 runtime OpenAPI 证据，达到 12/12。首次
全量回归还发现直接修改冻结 `nana_sidecar/app.py` 导致 D0 manifest 单项失败；现已
把 D1 组合移入新文件 `nana_sidecar/runtime_app.py` 并精确恢复 D0 app，随后 D0
49/49 manifest、70/70 D1 聚焦与 189/189 全量重新通过。最后复审未发现剩余
D1-06 阻断。

本单元没有实现 launcher、随机 loopback 端口、bootstrap token 单次交换、Host/
CORS/CSRF 完整 Local Web Plan B，也没有建设 scheduler/cancel/process runtime、
Capability/policy/budget、React、Tauri 或 alpha.1 算法内容。Artifact 目录仍为
CANDIDATE；没有 schema、数据迁移、架构路线或 D1 范围变化。下一工程单元为
D1-07 10,000 Event gate 与 D1 最终审查。

### 2026-07-30：D1-07 10,000 Event gate 与 D1 最终审查完成

D1-07 没有增加产品功能，只把 D1-01 至 D1-06 的真实 runtime 串入最终验证门：

- [x] 在一个 SQLite 事务中写入 10,000 个 Event 及 10,000 个 outbox 行后提交；
  Event 混合 `run.created/started/heartbeat/succeeded`、`plan.revised` 与
  `budget.updated` 六种类型，覆盖四个 aggregate；
- [x] 两个 Run 的 `aggregate_version` 与 `run_seq` 分别从 1 连续到 2,500，
  Plan 与 Workspace aggregate version 也分别从 1 连续到 2,500；
- [x] 通过受 Bearer session 与精确 Origin 保护的真实 HTTP SSE，按
  `3,333 → 断线 → 4,444 → 断线 → 2,223` 三段消费；
- [x] 每次只使用客户端最后确认的 `Last-Event-ID` 重连；三段实际收到的
  10,000 个 ID 与 SQLite 基准序列逐项一致，无缺口、无重复；
- [x] 重放完成后两个 Run projection 均为 `succeeded`，与 canonical `runs`
  表一致；消费端/UI-style projection 的 Event 类型计数、Plan revision、
  budget sequence 与最后 Event ID 全部精确一致；
- [x] 网络语义仍为稳定 Event ID + 至少一次传输 + 客户端按 ID 去重，不承诺
  网络层 exactly-once。

本工程单元的首个红测为 `tests.test_d1_runtime_gate` 模块不存在，按预期
import 失败；加入最小 gate 后 1/1 通过。完成全量扫描后集中发现并补正三项：

1. 原 gate 仅用严格递增、总数与末 ID 间接证明无缺口，现保存三次连接实际收到的
   完整 ID 序列并与数据库基准逐项比较；
2. 本文档总测试清单中的 `idempotency`、`DB commit 前后崩溃` 与 `blob 写半`
   曾仍为未完成，现按 D1-01/D1-05 的真实证据校正；
3. 11/12 号文档原先尚无 D1-07 与 D1 最终审查记录，现补齐。

最终复验使用 Python 3.12.13：D1-01 至 D1-07 聚焦 71/71，其中规格 8.3 六个
reconciliation 分支各 20 次、共 120 次故障注入，重复扫描幂等且读取
unavailable blob 为零；全量测试 190/190；`compileall`、Windows
`npm.cmd run check` 与 D0 49/49 manifest 全部通过。全量退出时仍有一个可由
既有 `tests.test_ui_smoke` 单独复现的 `gc` ResourceWarning；D1 SSE 独立运行
不产生该警告，按明确禁止项未修改旧 PySide6。

D1-01 至 D1-07 的唯一范围已经完成。Artifact 目录仍为 CANDIDATE；没有改变
schema、数据迁移、架构路线或 D1 范围，也没有提前建设 React 业务 UI、
scheduler/cancel/process runtime、Capability/policy/budget runtime、Tauri、
alpha.1 算法内容或旧 PySide6 CRUD 新功能。

### 2026-07-30：D1 全面复审补正完成

本次从已完成的 D1 checkpoint 开始，完整重读 00、05、06、07、10、11、12 号
权威文档；随后按 D1-01 至 D1-07、实现、测试、commit、证据的顺序只扫描不修改，
集中列出全部问题后再逐项补红测和修复。扫描确认原有七类 Artifact 失败边界、
规格 8.3 六分支各 20 次故障注入、Command 提交前后崩溃、HTTP SSE 无缺口和
10,000 Event 收敛证据均存在；另发现并关闭以下 7 组缺口：

1. Artifact reader 曾先按路径 hash、关闭后再重开，且最终只复验 size；同尺寸内容
   可在两步间替换后被返回。现改为同一打开句柄完成身份/hash 校验，并对实际返回
   bytes 再做 SHA-256 复验；
2. Artifact Store 曾允许祖先目录为 Windows junction/reparse point，真实探针可把
   `.partial` 写到 Workspace 外。现所有 stage/promote/read/quarantine/reconcile
   入口均拒绝受控 Artifact 路径中的 reparse point，真实 junction 复验已失败关闭；
3. rejected Command replay 曾直接信任 `error_json`。现按原 Command、Plan ID、
   expected/actual revision 和 canonical structured error 重新绑定，并确认引用的
   actual revision 存在；不一致时失败关闭；
4. 5000 位纯数字 `Last-Event-ID` 曾在范围判断前触发 Python 整数转换上限异常。
   现先以长度和字典序校验 SQLite integer 范围，HTTP 稳定返回 400；
5. 非 ASCII Bearer 值曾触发 `hmac.compare_digest(str, str)` 的 `TypeError`，且
   非 ASCII session token 配置可被接受。现 session token 只接受至少 32 字节的
   ASCII，认证以 bytes constant-time compare，非法值返回 401；
6. reconciliation grace 与 SSE poll/batch 曾接受 NaN、无穷、bool 或小数等无效
   配置。现全部要求有限、类型正确且位于有效范围，构造时失败关闭；
7. 原 D1-07 只证明精确 cursor 重连无重复，没有实际注入至少一次传输的重复 Event。
   现以落后一个 ID 的 cursor 重连，消费投影按稳定 ID high-water mark 去重，去重前后
   projection 完全不变，随后仍补齐全部 10,000 Event。

补充或扩展测试后集中运行 11 个定向 test method，初次按预期产生 14 个 failure
与 4 个 error；
最小修复后定向 12/12、D1 聚焦 77/77、全量 196/196。Python 3.12.13
`compileall`、Windows `npm.cmd run check`、D0 49/49 manifest 与冻结 digest
`6764afd4a6cfcacf0e89f6183be06fc078860b5790dc47fd1d9c5963b8f8d03f`
全部通过。D1 SSE 以 `-W always` 独立 13/13 无警告；全量退出的单个
`gc` ResourceWarning 仍可由既有 `tests.test_ui_smoke` 独立 13/13 复现，因此未修改
明确禁止范围内的旧 PySide6。

补正 commit 为 `830812cbdbb1a046ddd54165c5ecd1e0208de262`。提交后再次审查未发现
剩余 D1 阻断；D0 冻结文件、schema、OpenAPI、Artifact CANDIDATE 目录结构、迁移、
架构路线和 D1 范围均未改变，用户原有未提交修改未被清理、覆盖或提交。

### 2026-07-30：D1 验收结论边界与后续约束补正

前文“未发现剩余 D1 阻断”只表示 D1-01 至 D1-07 冻结验收范围通过，不表示下列
路线、D3 客户端和 launcher/sidecar 生命周期义务已经实现。补正状态如下：

- [x] **D0/D1 真实工时数值重估**：用户补录 D0=2 小时、D1=4 小时；按 4–6
  小时/日换算为 0.33–0.5 日和 0.67–1 日。D2 重估为 8–16 小时，D3 为
  6–12 小时；测试秒数、commit 时间戳和自然日跨度未计入，明细见 07 号文档；
- [x] **浏览器 SSE 客户端决策**：保持 D1 的 Bearer session 与精确 Origin 契约。
  D3 使用 `fetch + ReadableStream`，不使用无法附加 Authorization header 的原生
  `EventSource`；稳定 Event ID、至少一次传输、重连 cursor 与按 ID 去重不变。
  实际浏览器实现、preflight/Origin 失败关闭与断线测试仍属于 D3；
- [x] **单 reconciler 不变量**：D1 证据只对单 canonical Workspace owner、单
  reconciler、reconciliation 不与 commit/promote 并发的模型成立，不声称已经覆盖
  两个 reconciler 或 reconciler/commit 竞争；
- [x] **Workspace lock 生命周期实现**：sidecar 在可写打开 SQLite、migration/
  reconciliation 和 mutation serving 前取得 OS 级排他 lock，收敛后才 ready，并
  持锁至写者静止和 SQLite 关闭；第二实例失败关闭、崩溃释放与 ready 顺序均已有测试。

真实工时重估与浏览器客户端决策已经关闭；Workspace lock runtime 不阻断单 owner
测试约束下的 D2，但在锁实现前不得宣称多实例或并发 reconciliation 安全。若以后
允许并发，必须另开架构决策并增加竞争测试。这里没有实现 D2、D3 或 launcher
功能，也没有改变 D1 runtime、schema、Artifact CANDIDATE 目录或已提交 checkpoint。

### 2026-07-30：默认拒绝鉴权与权威导出补正

本次先扫描后集中确认两项缺口：

1. 最新 07、11、12 号权威文档只存在于 Vault，仓库导出不是相同 bytes，HEAD
   `830812cbdbb1a046ddd54165c5ecd1e0208de262` 不能重放最新验收结论；
2. `_LocalSessionMiddleware` 只硬编码保护 `/api/v1/contracts` 和
   `/api/v1/events`，其他新 HTTP 路由默认放行。D1 当前没有 mutation route，故
   既有 D1 runtime 仍可接受；但该模式在 D2 mutation serving 前必须停止。

按最小红测与实现补正：

- [x] 动态注册未来 `/api/v1/future-mutation` 后，匿名 POST 首次实际返回 200，
  红测以 `200 != 401` 失败，证明风险存在；
- [x] runtime 改为所有 HTTP 路由默认要求 Bearer session 与精确 Origin，只按精确
  路径放行 health、handshake 和必要文档入口；
- [x] 匿名 future mutation、未知 `/api/v1/*`、future mutation 尾斜杠和
  `/api/v1/contracts/` 尾斜杠全部在路由/redirect 前返回 401；
- [x] 同一 mutation 携带合法 session 返回 200；合法 session 跟随 contracts
  尾斜杠 redirect 后返回 200；已认证未知路径保持 404；
- [x] 07、11、12 号权威文档逐字节同步到仓库导出，并只把本轮明确文件加入提交。

Python 3.12.13 定向红测 1 个 failure；最小修复后定向 1/1、D1 聚焦 78/78、
全量 197/197。`compileall`、Windows `npm.cmd run check` 与 D0 49/49 manifest
均通过。全量退出仍只有既有 UI shutdown `gc` ResourceWarning；本轮未修改旧
PySide6。

Workspace lock 的后置 gate 不变：D2 可在单 owner 测试环境建设 scheduler、
process、policy 与 budget，但任何真实 mutation serving、第二实例或业务数据迁移
前，必须完成 OS 级排他锁、reconciliation 后才 ready、第二实例失败关闭、崩溃
释放/恢复，以及 SQLite/WAL 与 lock 的启动和关闭顺序测试。本次没有提前实现
launcher 或 Workspace lock。

### 2026-08-01：D2-00 至 D2-07 完成并共同验收

D2 在 D0 contract kernel 与 D1 Artifact/Event/HTTP-SSE runtime 上完成了首个受信、
冻结、极窄的本地执行闭环。共同 ACCEPT 的范围严格限定为
`python.unittest.locked` frozen worker；它不是通用不可信代码 sandbox，也没有开放
HTTP mutation serving、通用 shell、任意 Python、外部 publish/export 或 React UI。

| 阶段 | 状态 | 退出证据 |
|---|---|---|
| D2-00 authorization/storage hardening | 完成，纳入最终共同 ACCEPT | capability identity/digest、registry truth、safe JSON schema、Receipt/effect audit、locator privacy、schema v2。 |
| D2-01 scheduler/cancel gate | 完成，纳入最终共同 ACCEPT | 已授权 Action 单次 claim、竞争互斥、cancel 两阶段收敛、append-only Event/outbox、schema v3。 |
| D2-02 capability ceiling contract | 完成，纳入最终共同 ACCEPT | `python.unittest.locked` 的 read/write/network/env/process/timeout/default-effect ceiling。 |
| D2-03a registry/scope preflight | 完成，纳入最终共同 ACCEPT | schema v4 full registry JSON、digest round-trip、精确 process constraint、`project:source`/`project:tests` read roots。 |
| D2-03b admission service | 完成，纳入最终共同 ACCEPT | PolicyGrant/Approval 原子消费、args/action hash/registry binding、授权 Event/outbox 同事务。 |
| D2-04 locked executor | 完成，纳入最终共同 ACCEPT | 空环境、固定 argv/schema、audit guard、timeout/output/Receipt、Windows suspended-create→Job-bind→resume、真实孙进程 cancel。 |
| D2-05 runtime budget accounting | 完成，纳入最终共同 ACCEPT | schema v5 ledger、claim 前 reservation、Receipt usage 累计、runner/cancel/orphaned 保守结算。 |
| D2-06 security corpus gate | 完成，纳入最终共同 ACCEPT | 23 项手工 corpus；360 个 D2-effective + 100 个 supplemental；30 个真实孙进程 fixture；未授权 T3/T4/T4-like 通过数为 0。 |
| D2-07 D3 handoff | 完成，最终共同 ACCEPT | schema v6 append-only authorization snapshot、`D2RuntimeHandoff` v3、replay fixture、D3 消费边界。 |

对前文清单的同步判定：local scheduler、locked Python unittest、D2 窄执行面的
capability/schema/path/network/env/process/timeout/output、Run Budget、PolicyGrant、
Approval、Receipt、cancel race、child timeout、Approval 变化/过期/replay、越界路径、
shell metacharacter、未授权网络、输出上限和 Action replay 已有机器证据。通用项目文件
读取工具、scratch Artifact 写入、locked benchmark、Git 只读、公共网页读取、T3 导出、
完整 prompt/log/export canary、Provider 不可用、磁盘耗尽、通用 pause/resume 和 UI E2E
仍未完成，不因 D2 退出而自动勾选（以上为 D2 历史快照；D3 当前状态已在下方逐项复核并勾选）。

最终复验：聚焦回归 68/68；全量 269/269；以下七个 D2 测试模块共 55 tests 在
ResourceWarning-as-error 下通过：capability admission、run scheduler、locked executor、
budget accounting、security corpus、security matrices、runtime handoff。Python
compileall、TypeScript `tsc --noEmit`、evidence manifest 和隐私扫描全部通过。全量
suite 的既有 shutdown ResourceWarning 已隔离到迁移期 PySide6 UI smoke，不涉及 D2
Job、pipe 或 process handle。

D3 在开放真实 mutation serving 前仍必须完成 OS 级 Workspace lock、先持锁再可写打开
SQLite、reconciliation 后才 ready、第二实例失败关闭、SQLite close 后再释放 lock，
并单独完成 OpenAPI/runtime app 合流。D3 只能消费 D2 的 Action/Event/Receipt/outbox/
Artifact projection 与 authorization snapshot，不得重新推导授权或绕过 admission、
scheduler、executor。

### 2026-08-13：图片问题复核与 D3 退出重开

对 live worktree 逐项复核后，D3-01 至 D3-09 的历史窄范围实现和证据不删除，但
“完整 D3 退出”被重开。确认结果：

- [x] Workspace lock、ready order、第二 owner 失败关闭已有 runtime 测试；
- [x] React typed mutation、cancel、reload/reconnect、受控 T3 draft export、Receipt、
  10/10 成功旅程和 25/25 固定浏览器矩阵已有历史证据；
- [x] 真实启动器改为随机 loopback 端口并自动打开浏览器；
- [x] Bearer 不再输出 stdout；首个页面使用一次性 fragment secret 换取内存 session，
  fragment 立即清除，重放返回 401；刷新通过 HttpOnly same-site recovery cookie
  重新取得仅存于内存的 Bearer，产品 E2E 不再全局注入 Authorization；
- [x] 普通用户真实启动后完整走完一次主旅程并纳入 release matrix（当前 10/10 连续旅程）；
- [x] 独立 `PauseRun`/`ResumeRun`：allowlist、运行时、持久状态、UI 与浏览器验收均已覆盖；
- [x] `PauseRun`/`ResumeRun`/`CancelRun` 全部校验 `expected_revision`，冲突返回
  409 与真实 `actual_revision`；过期 Cancel 不触发内存取消信号；
- [x] `failed → retry_of`：StartRun request/UI/E2E 已绑定来源并持久写入字段与 Relation；
- [x] browser close/heartbeat/idle timeout/active Run/显式 Exit 生命周期；
- [x] 第二 launcher、恶意本地网页、CSRF/DNS rebinding/bootstrap replay 的完整产品矩阵；
- [ ] 用户能从 UI 解释每个状态的观察式证据：自动化可访问性与状态展示已通过，
  仍需真实用户完成 30 分钟观察并保存 `UsabilitySession` Artifact；
- [x] readiness 诊断会保留子进程退出码与 bounded stderr，不再只报固定五秒后的模糊错误；
- [x] 入口变更后的 D3 manifests、全量 Python、Vitest/build、10/10 与扩展 27/27 release matrix 已重新冻结。

当前冻结已通过 Python 421 passed / 2 skipped、67/67 Vitest、projection self-test、
TypeScript 检查、production build、10/10 连续完整旅程与扩展 27/27 浏览器 release matrix。
更早的 35/35、2/2、412 项与 25/25 数字只保留为历史快照。
# 2026-08-13 D3 当前执行状态

Pause/Resume 和 failed-to-retry 已进入纵向切片的契约、运行时、UI 与 Playwright 验收；Workspace/第二实例、崩溃恢复、刷新/关闭以及本地 Web 安全矩阵也已补齐。当前结果见 `C:\Users\q1968\Desktop\Nana\docs\CURRENT_D3_AUTHORITY.md`。
